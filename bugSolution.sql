```sql
SELECT * FROM employees WHERE department = 'Sales' AND salary >= 100000;
```
The corrected query uses `>=` to ensure that employees with salaries equal to or greater than 100000 are included in the results.